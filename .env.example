# Flask Environment
FLASK_ENV=development
FLASK_DEBUG=1

# Secret key (change in production)
SECRET_KEY=dev-key-change-in-production

# CORS origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5000

# API Settings
MAX_RECOMMENDATIONS=10
DEFAULT_RECOMMENDATIONS=5
# Data file (pickle) path. If using Git LFS, run 'git lfs install' and 'git lfs pull' on the server.
DATA_DIR=app/datos_procesados.pkl
# Optional remote URL to download the pickle if not present (use a direct binary URL, e.g., S3 pre-signed)
DATA_URL=
# OpenAI API configuration
OPENAI_API_KEY=***
# You can override model if needed, otherwise defaults to gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini
# If set to true/1/yes, the API will require the LLM to be enabled; otherwise falls back to deterministic text
AI_PERSONALIZER_REQUIRE_LLM=false
